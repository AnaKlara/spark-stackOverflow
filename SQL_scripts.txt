CREATE DATABASE stackOverflow;


create or replace type stringArray as varchar(64) array[20];
CREATE TABLE so_questions (
	id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	qid INT,
	question varchar(300),
	vots INT,
	answers INT,
	views INT,
	tags VARCHAR(63) ,
	created_at TIMESTAMP,
	last_modifyed TIMESTAMP
);

INSERT
  INTO
    so_questions (
    qid,
	question,
	vots,
	answers,
	views,
	tags,
	created_at,
	last_modifyed
	)
  VALUES (
  231401,
  'Porque estimativas de custos no PostgreSQL erram tanto?',
  46,
  0,
  1,
  'banco-de-dados,postgresql,índices',
  TIMESTAMP('2017-08-23-04.24.53'),
  TIMESTAMP('2021-07-08-17.58.55')
  );

SELECT 
  * 
FROM 
    so_questions;

SELECT 
    COUNT(*) row_count
FROM 
    so_questions
    
